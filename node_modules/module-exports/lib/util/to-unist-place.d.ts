/**
 * @param {FileState} state
 *   State.
 * @param {Node} node
 *   Node.
 * @returns {Position}
 *   Position.
 */
export function toUnistPlace(state: FileState, node: Node): Position;
/**
 * @param {FileState} state
 *   State.
 * @param {number} offset
 *   Offset.
 * @returns {Point}
 *   Point.
 */
export function toUnistPoint(state: FileState, offset: number): Point;
import type { FileState } from '../types.js';
import type { Node } from 'typescript';
import type { Position } from 'unist';
import type { Point } from 'unist';
//# sourceMappingURL=to-unist-place.d.ts.map