/**
 * @param {FileState} state
 *   State.
 * @param {Node} node
 *   Node.
 * @param {PlaceFields | undefined} [options]
 *   Node.
 * @returns {Point | Position}
 *   Position.
 */
export function maybePlace(state: FileState, node: Node, options?: PlaceFields | undefined): Point | Position;
import type { FileState } from '../types.js';
import type { Node } from 'typescript';
import type { PlaceFields } from '../types.js';
import type { Point } from 'unist';
import type { Position } from 'unist';
//# sourceMappingURL=maybe-place.d.ts.map